// Export an enumeration to define user permissions levels
export enum Permissions {
  ADMIN = "ADMIN",
  READ_ONLY = "READ_ONLY",
}

// Export an enumeration to define different loyalty user tiers
export enum LoyaltyUser {
  GOLD_USER = "GOLD_USER",
  SILVER_USER = "SILVER_USER",
  BRONZE_USER = "BRONZE_USER",
}
